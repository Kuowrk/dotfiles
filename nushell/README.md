On macOS, make sure to set symlinks to the default nushell config directory:

`ln -s ./* <default nushell config dir>`

You can find the default nushell config directory with `$nu.default-config-dir`.
